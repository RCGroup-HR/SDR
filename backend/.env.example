# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=tu_password_aqui
DB_NAME=sdr_web

# JWT Configuration (CAMBIAR EN PRODUCCI�N)
# Genera uno seguro con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CAMBIAR_ESTE_SECRET_EN_PRODUCCION
JWT_EXPIRES_IN=24h

# Frontend URL para CORS
FRONTEND_URL=http://localhost:5173

# Configuraci�n de seguridad
# En producci�n, cambiar NODE_ENV a 'production'
